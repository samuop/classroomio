name: Publish @classroomio/mcp to NPM

# Heredado del upstream: publica a NPM bajo el scope @classroomio, que no pertenece a
# este fork. Triggers de push/PR quitados; queda solo manual.
on:
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20.19.3
          registry-url: "https://registry.npmjs.org"

      - uses: pnpm/action-setup@v4

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build publishable MCP package
        run: pnpm mcp:build

      - name: Publish package
        if: github.event_name == 'workflow_dispatch'
        run: pnpm --filter @classroomio/mcp publish --no-git-checks --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
