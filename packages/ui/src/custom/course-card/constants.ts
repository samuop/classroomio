/**
 * La portada que se muestra cuando el curso todavía no tiene la suya.
 *
 * El archivo se llamaba `classroomio-course-img-template.jpg`: la foto es de
 * stock, sólo el nombre delataba el origen, así que se renombró. Esta constante
 * es la ÚNICA referencia que quedaba apuntando al nombre viejo y no vive en el
 * dashboard sino acá, en el paquete de componentes — por eso sobrevivió al
 * repaso. Si vuelve a renombrarse el archivo, este es el lugar.
 */
export const DEFAULT_COURSE_BANNER_IMAGE = '/images/course-img-template.jpg';
